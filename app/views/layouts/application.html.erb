<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Font Awesome -->
    <script src="https://kit.fontawesome.com/3e0cb08681.js" crossorigin="anonymous"></script>

    <!-- ✅ JavaScript (Rails UJS / webpacker) を追加 -->
    <%= javascript_pack_tag 'application', 'data-turbo-track': 'reload' %>
  </head>

  <body class="bg-white">
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
          <a class="navbar-brand fw-bold text-white" href="/">Bookers</a>

          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav ms-auto">
              <% if user_signed_in? %>
                <%= link_to user_path(current_user), class: 'nav-item nav-link text-white' do %>
                  <i class="fa-solid fa-house"></i> Home
                <% end %>
                <%= link_to users_path, class: 'nav-item nav-link text-white' do %>
                  <i class="fa-solid fa-users"></i> Users
                <% end %>
                <%= link_to books_path, class: 'nav-item nav-link text-white' do %>
                  <i class="fa-solid fa-book-open"></i> Books
                <% end %>
                <%= link_to destroy_user_session_path, method: :delete, class: 'nav-item nav-link text-white' do %>
                  <i class="fa-solid fa-right-from-bracket"></i> Log out
                <% end %>
              <% else %>
                <%= link_to root_path, class: 'nav-item nav-link text-white' do %>
                  <i class="fa-solid fa-house"></i> Home
                <% end %>
                <%= link_to about_path, class: 'nav-item nav-link text-white' do %>
                  <i class="fa-solid fa-link"></i> About
                <% end %>
                <%= link_to new_user_registration_path, class: 'nav-item nav-link text-white' do %>
                  <i class="fa-solid fa-user-plus"></i> Sign up
                <% end %>
                <%= link_to new_user_session_path, class: 'nav-item nav-link text-white' do %>
                  <i class="fa-solid fa-right-to-bracket"></i> Log in
                <% end %>
              <% end %>
            </div>
          </div>
        </div>
      </nav>
    </header>

    <main class="container mt-4">
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>">
          <%= message %>
        </div>
      <% end %>
      <%= yield %>
    </main>

    <footer class="text-center mt-5">
      <div class="container">
        <p>CopyRight Infratop.inc</p>
      </div>
    </footer>
  </body>
</html>
